apiVersion: v1
kind: ConfigMap
metadata:
  name: django-cm
data:
  POSTGRES_DB: djangodb
  POSTGRES_USER: djangodb_user
  DB_NAME: djangodb
  DB_USERNAME: djangodb_user
  DB_HOST: postgres-db
  PGDATA: /var/lib/postgresql/data/django/
